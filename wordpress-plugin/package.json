{
  "name": "helix-admin-wordpress-plugin",
  "version": "1.0.0",
  "private": true,
  "description": "Build helpers for the Helix WordPress plugin wrapper",
  "scripts": {
    "build": "echo 'Static WordPress plugin wrapper ready'",
    "package": "bash ../scripts/build-wordpress-plugin.sh"
  }
}
